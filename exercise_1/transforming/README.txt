For transforming of data
clean_tables.sql removes "not available" data and non-numeric values in ratings. 
	Only selects columns from data tables needed to answer questions of interest (and a few more)

join_tables.sql joins all tables into one for final use. 

